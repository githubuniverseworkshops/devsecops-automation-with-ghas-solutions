name: "My CodeQL config"

disable-default-queries: true

queries:
    - uses: ./custom-queries/code-scanning.qls
    - uses: security-and-quality
    - uses: ./custom-queries/go

paths-ignore: 
  - '**/test'
